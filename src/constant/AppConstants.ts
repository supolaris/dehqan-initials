import {AppColors} from './AppColors';
import {normalizeHeight, normalizeWidth} from '../utils/CommonFunctions';

export const borderWidth = 1;
export const borderRadius = 10;
export const activeOpacity = 0.5;
export const buttonBorderRadius = 5;
export const borderColor = AppColors.borderColor;
export const appPaddingHorizontal = normalizeWidth(20);
export const componentPaddingVertical = normalizeHeight(20);
export const componentPaddingHorizontal = normalizeWidth(15);
